# Week 01 강의 노트 — Introduction to Linux (오리엔테이션)

> 소스: 2026-09-02 강의 녹음 STT + 강의 슬라이드(`L00-courseIntroduction.pdf`, `_private/2026-2/system-programming/week-01/`). 담당교수: 박성훈(Seonghoon Park) — 이 학기 처음 부임해 진행하는 **본인의 첫 강의**라고 직접 언급.

## 1. 운영 정보

- **담당교수**: 박성훈(Seonghoon Park). 이 학기 연세대에 새로 부임, 이번이 교수로서의 첫 강의라고 밝힘(긴장되지만 기대된다고 언급).
- **TA**: 김건중(Gunjoong Kim) — 박사과정생. 과제·출석 등 문의는 TA 이메일로.
- **출석**: Y-Attend 앱으로 전자 출석. 오늘은 테스트 삼아 진행. 배점표상 10%지만 한두 번 결석은 크게 감점하지 않겠다고 함(연고전 등으로 빠져도 괜찮다는 뉘앙스) — 다만 **3분의 2 출석은 본인 방침이 아니라 학교 규정**이라 반드시 지켜야 한다고 강조.
- **평가**: 중간고사 + 기말고사, 프로그래밍 과제 2개(어쩌면 3개, "아마 2개"). (배점 구체 수치는 이번 오리엔테이션 강의에서 언급 안 됨 — syllabus.md의 기존 수치와 대조 필요.)
- **AI 정책**: 사용을 막을 수는 없지만, 과제는 먼저 스스로 풀어보고 막힐 때만 AI와 상의할 것을 강력 권장. "시행착오 자체가 커널 내부 원리를 이해하게 만드는 과정"이라고 설명.
- **교재**: 정식 교재(Bovet & Cesati, *Understanding the Linux Kernel*)는 출간된 지 10년이 넘어 오래된 리눅스 버전을 다룬다며 구매를 권하지 않음. 대신 최신 슬라이드 + 실제 최신 커널 소스(웹사이트)를 보라고 권장. 슬라이드 이해에 AI를 활용해도 좋다고 언급.

## 2. 과목 성격 — "시스템 프로그래밍"이지만 실체는 리눅스 커널 프로그래밍

- 이 과목의 목표는 OS 커널 프로그래밍의 실전적 측면, 특히 **리눅스 커널**을 배우는 것. 운영체제(OS) 수업을 아직 안 들었다면 먼저 듣고 오길 권장 — OS 수업은 이론 위주로 실제 리눅스 코드를 거의 안 다루는 반면, 이 과목은 **실제 코드**를 다룬다는 게 핵심 차이.
- 배우는 내용: 커널 내부 동작 원리, 커널 커스터마이징 방법. 과제는 커널 수정 또는 커널 모듈 작성 형태.
- 강조점: 리눅스 이해는 커널/시스템 프로그래밍에 국한되지 않고, 상위 레벨 애플리케이션·AI 개발에도 밑바탕이 된다 — "OS가 어떻게 동작하는지 알아야 그 위의 것들을 제대로 만들 수 있다."
- 왜 리눅스인가: 무료·개방(비용 없음), 약 30년간 개발되어 온 성숙한 OS. 스마트폰·워치·가전 등 모든 곳에 있다는 점을 강조("Linux is everywhere").

## 3. 이번 학기 개요 (구두로 설명된 커리큘럼)

기존 OS 수업 개요와 유사하지만, 이론이 아니라 **실제 리눅스 코드** 기준이라는 게 차이. 다룰 주제: 리눅스 프로세스, 스케줄링, 시스템 콜, (커널) 동기화, 메모리 관리, 파일 시스템 등.

## 4. 스마트폰 OS 생태계 맥락 (도입 동기 부여용)

- 스마트폰 OS: 안드로이드, 아이폰, (과거) 심비안, 윈도우 10 모바일, webOS, 블랙베리, 타이젠 등 — 대부분 **리눅스 기반**(무료 + 잘 관리됨). webOS(LG)·타이젠(삼성)은 스마트폰에서는 밀려났지만 지금은 TV·냉장고 등 가전제품에서 쓰인다.
- 안드로이드/타이젠 스택: 커널(하위, CPU 스케줄링·메모리 관리·파일시스템)과 프레임워크(상위, 애플리케이션 레벨)를 구분. 두 스택 모두 최하단은 리눅스 커널. LG·삼성·구글 같은 기업에서 이 레이어를 다루게 될 수 있다는 커리어 연결 언급.

## 5. 교수 본인 연구 소개 (Mobile AI and Systems Lab)

- 연구실명: **Mobile AI and Systems Lab**. 비전: 증강현실용 실시간 주변 상황 이해, 몰입형 인터랙티브 3D 콘텐츠(Meta Quest 등 XR 기기) 같은 고급 모바일 경험을 사용자에게 제공하는 것.
- 기술적 난제: vision foundation model, vision-language model, LLM, NeRF(Neural Radiance Field), 3D Gaussian Splatting 등은 연산량이 매우 크다. 목표는 이런 무거운 작업을 **자원이 제한된 모바일 기기에서 실시간으로** 돌리는 것 — 서버급 GPU 대비 훨씬 느리고, 배터리·발열 제약까지 있다.
- 최적화 전략: 스마트폰 AP의 NPU/CPU/GPU 간 작업 분배, 동작 주파수(DVFS) 제어, GPU-NPU 세밀 스케줄링. 여기에 AI 쪽 기법(static model configuration, multi-exit/stitchable network 같은 dynamic neural architecture, scalable scene representation)을 결합. **이 모든 최적화가 결국 OS/안드로이드 내부 동작에 대한 깊은 이해를 전제**로 한다는 게 이 소개의 핵심 메시지 — 왜 우리가 커널을 배워야 하는지에 대한 교수 본인의 답.
- 스케줄링·메모리 관리의 세부 내용은 "학부보다는 대학원/박사 수준"이라며 이번 강의에서는 다루지 않는다고 명시. 관심 있는 학생은 인턴/박사과정 지원 문의 환영이라고 언급(연구실 홍보).

## 6. 마무리

과목 개요 소개를 마치며 질문 받음. 1주차는 자유롭게 수강철회 가능하며, 첫 주는 전원 출석으로 처리하겠다고 안내.

---

# Day 2 (2026-09-05) — 리눅스 커널 버전 체계 & 프로세스 개념 입문

> 소스: 2026-09-05 강의 녹음 STT(whisper-1) + 강의 슬라이드 `L01-linuxprocesses.pdf`(`_private/2026-2/system-programming/week-01/`). 이 강의의 도입부(§7)는 슬라이드 없이 음성으로만 진행됐다 — 대응하는 슬라이드 파일을 받지 못해 전사본만을 근거로 정리했다. §8부터는 `L01-linuxprocesses.pdf`와 대응된다. **이 강의는 슬라이드 87장 중 약 앞쪽 40여 장(리눅스 프로세스 리스트 도입부까지)만 다뤘다 — pidhash 테이블 세부, Task Lists Big Picture, Wait Queues, Process Usage Limits, Process Switching 내부 동작(switch_to 어셈블리), Kernel Threads, Process 0/1, Destroying Processes 등 뒷부분 슬라이드는 이번 강의에서 다루지 않았으며 다음 수요일로 예고됐다. 이 노트는 그 뒷부분을 추측해서 채우지 않는다.**

## 7. 리눅스 커널 버전 체계 (도입부, 슬라이드 미제공)

- **역사·사용 현황**: 리눅스는 1995년경 출시(교수 발언 그대로 — "내가 1994년생이라 리눅스는 내 친구 같은 존재"). 개인 데스크톱/노트북 점유율은 약 2%에 불과하지만, 서버·스마트폰·엣지 디바이스·가전제품에서는 압도적으로 많이 쓰인다. 전체 컴퓨팅 기기 중 스마트폰 비중이 현재 약 70%.
- **오픈소스 프로젝트로서의 특징**: 라이선스 제약이 거의 없어 누구나 코드를 읽고 커널을 쓰고 심지어 직접 OS를 만들어볼 수도 있음. 약 33년 된 소프트웨어로 매우 성숙·잘 관리됨. 인텔·삼성·구글 등 대기업 포함 다수 개발자가 기여.
- **C 언어로 작성됨**: 대부분 C 프로그래밍을 어려워하지만, 리눅스는 C를 배우는 최고의 예제라는 게 이 과목이 존재하는 이유 중 하나.
- **라이선스**: GPLv2 — 자유롭게 수정·배포 가능, 상업적 사용도 가능하나 소스 공개·저작권 표시 조건이 있음("근데 큰 문제는 아니라고 생각한다").
- **성장 규모**: 약 4천만 줄까지 성장, 두 달마다 약 40만 줄씩 계속 증가, 릴리스당 약 2,000명 개발자 기여.
- **릴리스 버전 체계**: RC(release candidate, 실험적) → mainline(2~3개월마다, 리누스 토르발스가 결정) → stable(mainline 위에 버그 수정, major.minor.stable 형식) → **LTS**(long-term support, 가장 안정적, Chrome OS·안드로이드·윈도우·아마존 리눅스 등 실제 제품이 사용. 약 1~2년마다 릴리스, 유지보수 기간이 길게 연장됨 — 일부는 최대 6년까지). LTS가 중요한 이유: 제품 입장에서는 새 기능보다 검증된 커널이 훨씬 중요하기 때문.
- **kernel.org**: 최신 mainline/stable/RC 버전을 항상 확인 가능한 사이트. 강의 시점(2026-09) 기준 mainline 7.2, stable 7.2.2, RC 7.3-RC1(슬라이드 날짜가 안 맞아 교수가 직접 정정). LTS는 6.18이 최신(작년 11월 릴리스), LTS는 대략 매년 10~12월경 릴리스.
- **실제 제품 사례**: 안드로이드가 가장 널리 쓰이는 LTS 기반 제품, 그 외 우분투·Chrome OS·WSL·라즈베리파이·아마존 리눅스도 LTS 기반. 스마트폰은 커널 교체 불가하지만 아마존 리눅스/우분투는 더 최신 커널로 교체 가능.
- **안드로이드 공용 커널**: 안드로이드도 자체 커널이 있지만 LTS 리눅스 커널 기반. LTS → 안드로이드 메인라인 커널로 지속적인 머지가 이뤄지고, 그 메인라인 위에서 버전별 안드로이드 커널·OS가 개발되는 브랜칭 모델.
- 이 도입부 내용은 시험(중간/기말) 대상이 아니라고 명시. "지금부터가 진짜 강의이고 시험 범위"라며 Linux Processes로 전환.

## 8. Kernel Outlook (커널 레벨 구성요소)

- User Level(사용자 프로그램: 브라우저·셸·터미널 등) → System Call Interface → Kernel Level의 각 매니저:
  - **Filesystem Manager**(Ext2fs, proc, nfs) — 스토리지의 파일을 읽고 쓰는 것
  - **Process Manager**(Task Management, Scheduler, Signaling) — 교수는 이걸 memory manager와 함께 가장 중요한 컴포넌트로 꼽음
  - **Memory Manager** — 가상 메모리를 물리 메모리로 매핑
  - **Network Manager**(IPv6, ethernet), **Device Manager**(block, character) — Buffer cache를 매개로 서로 연결
- 최하단 HW Level(CPU, GPU, 메모리, 스토리지, 키보드·마우스 등)까지 커버하는 게 이 학기 전체의 목표.

## 9. Linux Cross Reference (LXR) — elixir.bootlin.com

- 리눅스 커널의 최초 버전부터 최신 버전까지 코드를 검색·열람 가능한 웹 사이트. 변수·매크로·함수를 검색하면 정의 위치와 참조·사용 위치를 바로 보여줌.
- 세부사항은 바뀌어도 핵심 개념은 그대로라서 교재 구매를 권하지 않음(그래도 읽어볼 가치는 있다고 언급). 과제 진행 시 이 사이트를 매우 자주 쓰게 될 거라고 예고(과제 세부는 아직 미확정).

## 10. 도입 질문 — 커널 코드도 프로세스인가?

- "커널 코드는 프로세서가 실행한다는 점에서 일반 프로그램과 똑같이 동작하고, 자주 제어권을 넘겼다가 프로세서로부터 다시 돌려받는다." 그렇다면 커널 코드 자체가 process인가? 그렇다면 어떻게 제어되는가?
- 답("process는 실행 중인 프로그램의 인스턴스")은 지금은 헷갈릴 수 있다며 뒤에서(§13) 다시 설명하겠다고 예고.

## 11. OS 실행 모델 두 가지 — Process-based OS vs Execution within User Process

- **Process-based OS**: OS 기능도 사용자 프로세스들과 나란히 별도 프로세스로 돎. 프로세스 간 전환(풀 프로세스 스위치)은 한 건물에서 나가 다른 건물로 걸어 들어가는 것과 같아 오버헤드가 큼(교수의 건물/아파트 비유).
- **Execution within User Process**(리눅스가 택한 모델): OS 기능이 사용자 프로세스 자신의 컨텍스트 안에서 실행 — 같은 건물 안에서 위/아래층을 오가는 것에 가까워 더 효율적.
- **대가**: 사용자 입장에서 주소 공간 일부를 커널에 내줘야 함. 32비트 시스템 기준 가상 주소 공간 4GB 중, process-based OS라면 사용자가 4GB를 다 쓸 수 있지만 execution-within-user-process에서는 3GB(사용자) / 1GB(커널)로 나뉨 — 모든 프로세스가 주소 공간의 25%를 내주는 셈. 교수의 비유: 커널/OS = 건물주(land lord), 사용자 프로그램 = 세입자(tenant), 세입자는 건물주 규칙을 따라야 함.
- **얻는 이득**: 이 방식은 커널 관련 작업만 할 때 비용이 큰 풀 컨텍스트 스위치를 피하고, 대신 같은 프로세스 안에서 훨씬 저렴한 **mode switch**만 하면 됨 — 이게 이 모델의 핵심 장점("virtually all OS code gets executed within the context of a user process").
- 휴식 후 다시 강조한 정리: 장점 = mode switch 오버헤드가 낮음 / 단점 = 사용자 입장에서 주소 공간 일부(1GB)를 아예 못 씀 — "중요한 내용이라 다시 짚고 싶었다"고 직접 언급.

## 12. Dual-Mode Operation — User Mode / Kernel Mode

- 최신 CPU는 최소 두 가지 모드(user/kernel)를 구분하는 하드웨어 지원 제공. (세부적으로 더 많은 레벨을 지원하는 아키텍처도 있지만 이는 매우 architecture-specific이라 이 과목에서는 이 두 모드만 다룸.)
- **User mode**: 사용자를 대신해 실행, 물리 메모리·파일 등 커널 요소에 직접 접근할 권한 없음.
- **Kernel mode**: OS를 대신해 실행, 프로세서·명령어·레지스터·메모리에 대한 완전한 제어권. 사용자 프로그램이 CPU를 직접 제어하게 두면 매우 위험하기 때문에 이렇게 분리됨.
- 구체적 예시(파워포인트): 그냥 켜져서 돌아가는 동안은 user mode. 클릭/타이핑하거나 런타임 에러가 나면 유저 프로그램이 스스로 처리 못 하는 일이라 이때 mode switch가 일어남. 풀 프로세스 전환(스케줄러가 완전히 다른 프로세스를 고르는 것)은 정말 필요할 때만 발생 — 이게 이 설계의 큰 장점.

## 13. Mode Switch를 유발하는 3대 이벤트

1. **Hardware Interrupt**: 키보드 입력, 네트워크 패킷 도착 등 — 유저 프로그램이 스스로 처리 불가.
2. **Software Interrupt (exception)**: divide-by-zero(매우 심각), page fault(물리 메모리에 매핑 안 된 영역 접근 시도) 등 런타임 에러 — 유저 프로그램이 보고 싶어 하는 상황도 아니고 스스로 처리도 불가.
3. **System Call**: 유저 프로그램이 능동적으로 원하는 작업 — 파일 읽기(예: 파워포인트가 .ppt 파일을 열 때), 프로세스 생성(fork — 요즘 프로그램 대부분이 멀티프로세스), 네트워크 통신(소켓/TCP-IP, 네트워크 매니저가 처리).
- **워크드 예제 — printf → write 시스템 콜**: user mode의 프로그램이 C 라이브러리의 `printf()`를 호출 → 내부적으로 `write()` 시스템 콜 인터페이스 호출 → 이 시점에 mode가 커널로 전환됨 → 커널 코드가 시작되고 실제 하드웨어(모니터 등)와 통신 → 화면에 출력이 나타남.
- (여기서 3시 15분경 짧은 휴식.)

## 14. 프로세스의 정의

- 교과서 정의: "process는 실행 중인 프로그램의 인스턴스(an instance of a running program)." 교수는 이 정의가 요즘은(멀티프로세스/멀티스레드 프로그램이 흔해서) 헷갈릴 수 있다고 지적.
- 교수가 실제로 ChatGPT에게 재정의를 요청해서 얻은 표현: **"process는 자기만의 virtual address space와 자원(resources)을 가진 실행 중인 인스턴스."** 프로세스가 자기만의 가상 주소와 자기만의 자원을 갖는다는 점을 잘 짚어줘서 이 표현이 마음에 든다고 언급.
- 프로세스가 포함하는 것: **images**(code, data, stack, heap — 가상 메모리에 저장) + **process context**:
  - **Program context**: data registers, program counter(PC), stack pointer(SP)
  - **Kernel context**: PID, GID, SID, VM 구조체, 열린 파일, 시그널 관련 정보

## 15. 프로세스 이미지 레이아웃 — code/data/stack/heap

- 코드 영역과 데이터 영역: 데이터는 소스 코드 작성 방식에 따라 **initialized**/**uninitialized**로 나뉨 — 예: `int i;`(대입 없음, uninitialized) vs `int i = 1;`(initialized).
- **Heap**: 동적 메모리 할당(malloc류)용, 아래에서 위로 자람. **Stack**: 자동/임시 변수·리턴 주소·호출 환경(C 코드의 일반적인 변수들), 위에서 아래로 자람.
- **워크드 예제(실제 C 코드)**: 전역 배열(크기 100 고정이라 uninitialized data 영역에 들어감, 동적 할당은 아님), 전역 변수 `bufsize`(initialized data 영역), `main()`/`f1()` 함수 코드(text 영역, OS가 한 줄씩 읽으며 실행), `f1()` 안의 `malloc(bufsize)` 호출(heap에 할당), `main()`의 지역 변수 `i`/`buf`(stack에 저장). 정리: 전역 변수 → data 영역, 지역 변수 → stack, 코드 → code 영역, malloc류 결과물 → heap.
- **init 프로세스(process 1) 예시**: 리눅스 부팅 시 최초로 시작되는 프로세스. 자기만의 user stack, 데이터 구조, heap, code를 가지며, 커널 주소 공간에는 이 프로세스의 커널 쪽 데이터 구조 일부가 저장됨.

## 16. 프로세스 모델의 한계 → 스레드가 필요한 이유

1. **Cooperating processes 문제**: 스레드 개념이 아예 없다고 가정(초기에는 실제로 그랬음) — 많은 애플리케이션이 여러 작업을 동시에 처리해야 하는데, 순수 프로세스들은 주소 공간·자원을 공유하지 않아 매우 비효율적.
2. **Multiprocessing 문제**: 전통적인 프로세스는 한 번에 CPU 코어 하나에서만 돎 — 기기에 멀티코어가 있어도 순수 멀티프로세스 모델만으로는 이를 활용할 수 없음.

## 17. Process vs Thread — 무엇을 공유하는가

- 전통적 관점: **process = process context + (code, data, stack)**. 스레드가 등장하면: 각 스레드는 자기만의 논리적 실행 흐름을 갖지만(당연히), **code·data·kernel context는 공유**하고 **state(구체적으로 stack)만 스레드별로 다름**. 즉 process와 thread의 차이는 결국 "무엇을 공유하느냐"로 귀결됨.
- 구체적으로: 프로세스는 자기만의 virtual memory와 자원(stack/heap/data/code/kernel context)을 가짐. 한 프로세스가 여러 스레드를 가지면 그 스레드들은 heap·data·code를 공유하고, 각자 온전히 갖는 건 stack뿐. 스레드가 없는 순수 프로세스들은 parent-child-sibling 계층은 갖지만 아무것도 공유하지 않음(각자 완전히 별도의 code/data/context/stack).
- **생성 방식**: process는 `fork()` 같은 시스템 콜로, thread는 표준 API인 **pthread**로 생성. pthread는 인터페이스/API일 뿐이고 실제 구현은 라이브러리 개발자 몫(입출력 규격만 정의) — 오늘날 대부분의 멀티스레드 애플리케이션이 pthread 라이브러리로 작성됨(예: `pthread_create()`).
- **통신 비용**: 별도 프로세스끼리 통신하려면 **IPC**(inter-process communication)가 필요해 오버헤드가 있지만, 스레드는 code/data를 공유하므로 IPC가 필요 없음 — 이게 리눅스가 스레드 개념을 도입한 이유 중 하나.
- **동기화**: 멀티프로세스는 각자 완전히 분리된 주소 공간이라 더 안전하지만, 스레드는 메모리·데이터 구조를 공유해서 한 스레드가 공유 자원에 다른 스레드가 원치 않는 작업을 할 수 있는 문제가 생김 — 그래서 pthread API가 **mutex**, **condition variable** 같은 동기화 메커니즘을 정의.

## 18. Process States

- **일반(교과서) 모델**: **new**(생성 중, 일시적 1회성) → **ready**(스케줄될 준비는 됐지만 아직 기회를 못 받음) → **running**(스케줄러가 골라서 실제 CPU에서 실행 중 — 최신 CPU 스케줄러는 공정한 시간 분배를 위해 짧은 시간 후 다시 ready로 되돌림) → **exit**(종료). 추가로 **blocked**: 특정 이벤트(네트워크·키보드·시간 경과 등)를 기다리는 동안 CPU에서 돌지도, ready 상태도 아닌 상태(예: 프로그램이 10초를 기다리는 경우).
- **리눅스 구현**(최근 커널에서 갱신됨, 교수의 간략한 프레이밍): `TASK_RUNNING`이 ready와 running을 모두 포괄. blocked 프로세스는 `TASK_INTERRUPTIBLE` 또는 `TASK_UNINTERRUPTIBLE`(둘 다 특정 이벤트를 기다리며, 이벤트 발생 시 다시 스케줄 가능해짐). `TASK_STOPPED`은 이 자리에서 exit에 가까운 상태로 간단히 언급됨(자세한 exit_state 세분화는 다루지 않음).

## 19. task_struct / thread_info — Process Control Block(PCB)

- `sched.h`가 프로세스·CPU 스케줄러 관련 데이터 구조를 정의하는 핵심 파일. **task_struct** = 리눅스 프로세스를 나타내는 데이터 구조, 즉 **PCB(process control block)** — task information, task image(code/data/stack/heap), program context를 담음.
- 리눅스에서는 process를 **task**라고 부르며, thread도 task다 — task가 CPU 스케줄러가 다루는 단위. (교과서 관점 "process" vs 리눅스 관점 "task"라는 용어 혼용에 유의하라고 언급.)
- 버전 비교: Linux 2.6.11 시절 task_struct는 짧았지만, 계속 필드가 추가되면서 최신 버전은 거의 1,000줄에 달함(기본 개념은 유사). **task_struct는 architecture-independent**, 반면 **thread_info는 architecture-dependent**(Intel·ARM 등이 각자 설계) — 실제 CPU와 밀접하게 연관되기 때문. 최신 LTS 기준으로도 ARM/x86 thread_info를 각각 비교해서 보여줌.
- 모든 프로세스가 자기만의 process descriptor를 가져야 하는 이유: 커널이 모든 process/task를 식별·관리해야 하고, 각 task/context가 자기 정보(process context, code, data 등)를 저장할 자기만의 디스크립터가 필요하기 때문.

## 20. User Stack / Kernel Stack

- Dual-mode 동작은 커널도 자기만의 스택이 필요하다는 뜻 — 커널 코드 실행 중 지역 변수를 저장할 곳이 **kernel stack**.
- 커널 모드 진입 시(mode switch 발생 시) task의 레지스터(스택 포인터, 인스트럭션 포인터 등)가 kernel stack에 저장됨 — 나중에 다시 user mode로 돌아갈 때 프로그램이 정확히 어디까지 진행됐는지 알기 위함. 커널 함수 실행 중의 지역 변수도 kernel stack에 저장됨.
- 프로세스당 할당된 작은 커널 메모리 영역(예전엔 thread_info + kernel stack 두 가지를 담았으나, **요즘은 thread_info가 여기 저장되지 않아 kernel stack만 남음**, 8KB 영역). 모든 사용자 프로세스는 스택 2개(user, kernel)가 필요하며, 커널 스택은 커널 데이터 세그먼트에 위치하고 스택 전환은 mode switch 시점에 일어남.

## 21. "current" 프로세스 식별 — 옛날 방식 vs 현대 방식

- 커널이 "지금 실행 중인 프로세스가 뭔지" 알아야 하는 이유: 커널 모드가 끝나면 다시 user mode로 돌아가야 하는데, 그게 정확히 어느 프로세스였는지 알아야 하기 때문.
- **Linux 2.6.11**: 복잡한 방식 — 어셈블리 레벨의 스택 포인터 마스킹으로 커널 스택 하단의 정보에서 현재 프로세스를 알아냄.
- **현대 방식**: 각 CPU/코어가 현재 실행 중인 task를 가리키는 포인터를 담은 자기만의 데이터 구조를 유지 — CPU가 current task를 바로 알 수 있음(Linux 6.18.48의 architecture-dependent 코드로 예시 제시).

## 22. Process List

- 리눅스 커널은 한 번에 많은(교수 추정 "천 개 정도?") 프로세스를 돌리므로 이를 효율적으로 관리할 방법이 필요.
- **이중 연결 리스트(doubly linked list)**: 각 task_struct가 prev/next 포인터를 가지며 `init_task`에서 시작해 다른 프로세스들이 연결됨. 커널이 삽입·삭제·스캔용 매크로 제공 — 예: `for_each_task`(다음 태스크 포인터를 계속 따라가며 전체 순회).
- **한계와 pidhash 도입 동기(미완)**: 연결 리스트 순회만으로 특정 PID(예: PID 1000)를 직접 찾으려면 첫 태스크부터 순차적으로 최대 1,000번 스캔해야 해서 매우 비효율적 — "그래서 리눅스 커널은 해시 테이블(pidhash)도 함께 유지한다"고 언급한 직후, 시간 관계상(4:46) 강의를 마무리. **pidhash의 실제 구조·조회 방식 등 세부 내용은 이번 강의에서 다루지 않았고, 다음 수요일 강의("process list and related things")에서 이어가겠다고 예고했다 — 이 노트는 그 내용을 추측해서 채우지 않는다.**

---

## Day 3 (2026-09-09) — 프로세스 조직, 프로세스 스위칭, 스레드

> 소스: 같은 슬라이드(`L01-linuxprocesses.pdf`, 42쪽 이후 계속). 초반에 지난 시간 내용(프로세스/스레드 정의, task_struct, 프로세스 상태)을 짧게 복습.

### 23. 프로세스 목록 관리 — 연결 리스트 + 해시 테이블

- 커널이 프로세스들을 관리하는 두 자료구조: **연결 리스트**(전체 순회·삽입·삭제용, `for_each_task` 같은 매크로 제공)와 **해시 테이블**(특정 PID를 빠르게 찾기 위한 것 — 리스트만으로는 순차 탐색이라 비효율적이라는, 지난 시간 예고했던 pidhash 도입 동기가 여기서 이어짐).
- **프로세스 간 관계**: 부모(parent, 포인터 1개 — 프로세스마다 부모는 정확히 하나), 자식(children, 연결 리스트 — 여러 개 가능), 형제(siblings, 연결 리스트)로 계층 구조를 유지.

### 24. Run Queue와 Wait Queue

- CPU 관점에서 프로세스는 **runnable(ready/running)** 아니면 **blocked** 상태 중 하나. 이를 관리하는 두 자료구조:
  - **Run queue**: CPU 스케줄러가 다음에 돌릴 프로세스를 고를 때 순회하는 큐. CPU(코어)마다 자기만의 run queue를 가지며, 그 안에 스케줄링 알고리즘별로(CFS, RT, DL 등) 별도 리스트를 유지.
  - **Wait queue**: blocked(task interruptible/uninterruptible) 상태인, 즉 특정 하드웨어·디바이스·타이머 등 이벤트를 기다리며 잠들어 있는 프로세스들의 집합. 해당 이벤트가 발생하면 디바이스 드라이버나 커널 서브시스템이 프로세스를 깨워서(wake up) run queue로 옮긴다.
- (부수적으로) 프로세스별 자원 사용 한도(최대 CPU 시간, 파일 크기, heap/stack 크기 등)도 운영체제가 지정한다는 언급 — "중요하지 않다"고 짧게 넘어감.

### 25. Process Switching — 스위칭을 유발하는 4가지 이벤트

프로세스(task) 스위칭이 일어나는 4가지 경우:
1. 프로세스가 스스로 sleep 상태로 들어갈 때(runnable → blocked).
2. 프로세스가 종료(terminate)될 때.
3. 시스템 콜에서 유저 모드로 복귀하려는데, 현재 프로세스가 다음에 돌기 가장 적합한 프로세스가 아닐 때(더 높은 우선순위나 더 낮은 virtual runtime을 가진 다른 프로세스가 있을 수 있음 — 커널이 굳이 원래 프로세스로 돌아갈 이유가 없다는 논리).
4. 인터럽트 처리를 마치고 유저 모드로 복귀하려는데, 마찬가지로 현재 프로세스가 최선이 아닐 때(3번과 사실상 동일한 논리).
- 이 네 이벤트가 발생하면 커널은 컨텍스트 스위치 여부를 결정하고, 이전 프로세스(prev)의 컨텍스트를 저장한 뒤 다음 프로세스(next)를 스케줄링해 그 컨텍스트를 복원.

### 26. Hardware Context와 `thread_struct`, `switch_to` 매크로

- 리눅스에서는 프로세스도 스레드도 "task"라 부르며(스케줄링 단위), "process switching"보다 **"task switching"(context switching)**이라는 표현이 더 정확.
- **하드웨어 컨텍스트**: CPU가 프로세스를 실행하는 데 필요한 레지스터 값들(스택 포인터, 명령어 포인터 등) — 프로세스를 재개하려면 이 값들을 CPU 레지스터에 다시 로드해야 한다.
- 하드웨어 컨텍스트는 **두 곳에 나뉘어 저장**된다: 일부는 프로세스 서술자(`task_struct`) 안의 **`thread_struct`**(아키텍처 종속적인 부분 — 예: EIP/ESP)에, 나머지는 **커널 모드 스택**에. `task_struct` 자체는 아키텍처 독립적(core 코드가 관리)이지만, CPU 레지스터 관련 정보는 아키텍처마다 달라서 별도 구조체(`thread_struct`)로 분리해뒀다는 설계 이유를 설명.
- 컨텍스트 스위치는 두 단계로 구성: **(1) 커널 모드 스택 전환**, **(2) 하드웨어 컨텍스트 전환**(리눅스 커널의 **`switch_to` 매크로**가 담당, 어셈블리로 작성됨 — 성능 때문. 최근 커널은 일부를 Rust로 옮기는 추세지만 이런 초저수준 코드는 여전히 어셈블리라고 언급).
- **`switch_to`의 동작을 단계별로 설명**:
  1. 몇몇 레지스터 값(flags, EBP 등)을 이전 프로세스(prev)의 커널 모드 스택에 저장.
  2. prev의 스택 포인터를 prev의 `thread_struct`에 저장 — **스택 포인터 자체는 커널 스택에 저장할 수 없다**(그 값이 바로 "커널 스택이 어디인지"를 가리키는 값이라, 스택 안에 넣으면 위치를 잃어버리기 때문 — 그래서 일부는 스택에, 일부는 전역 구조체에 나눠 저장하는 이유가 여기서 나온다).
  3. next의 스택 포인터를 next의 `thread_struct`에서 읽어와 CPU 레지스터에 로드 — 이 시점에 커널이 next의 커널 스택 영역을 식별할 수 있게 됨.
  4. 나머지(특히 명령어 포인터, instruction pointer)는 `__switch_to` 매크로가 처리 — "프로세스가 어디까지 진행됐는지"를 나타내는 값이라 마지막에 복원.
  - 이 스텝들이 끝나면 CPU 레지스터가 next 프로세스의 상태를 가리키게 되어 실제 전환이 완료.

### 27. 스레드(Thread) — 왜 만들어졌는가

- **전통적 모델(스레드 없음)**: 자원 소유(리소스: 가상주소공간, 프로세스 컨텍스트)와 실행(execution)이 프로세스 하나에 묶여 있었음 — 프로세스 = 자원 + 실행 스레드 1개.
- **현대 모델(스레드 있음)**: 자원과 실행을 분리 — 여러 스레드가 자원(주소공간, 파일 핸들 등)은 공유하되 각자 자기만의 실행 흐름(스택, 컨텍스트)을 가짐.
- **도입 동기**: 스레드 없이 애플리케이션 내부의 병렬성을 활용하려면 병렬로 돌려야 할 부분마다 별도 프로세스를 만들어야 했는데, 프로세스끼리는 주소공간·자원을 전혀 공유하지 않아 매우 비효율적이고 다루기 어려웠음. 스레드는 이 문제를 해결 — 코드/데이터 등 공통 컨텍스트는 공유하되 스택·실행 컨텍스트만 각자 가짐.
- 스레드도 (프로세스처럼) **스케줄링의 단위**이며, 리눅스 커널 관점에서는 프로세스와 스레드 모두 "task"로 취급되고, 둘의 구분은 "얼마나 많은 걸 공유하느냐"의 문제일 뿐.
- **구현 방식 두 가지**:
  - **User-level threads**: (예: 직접 만든 파이썬/자바 스레드 라이브러리) 커널은 이 존재를 전혀 모름 — 커널 입장에선 여전히 프로세스 하나(스레드 하나)로 보임. 문제: 커널이 스케줄링을 못 해준다.
  - **Kernel-level threads**: 커널이 스레드를 직접 스케줄링 단위로 인식 — 더 현실적인 방식이라는 교수의 평가. **리눅스는 kernel-level threads를 쓴다.**
- 3시 47분, 시간 관계상 이 시점에서 강의 마무리, 남은 시간은 질의응답.

---

## Day 4 (2026-09-11) — 스레드의 커널 구현, 프로세스 소멸, 프로세스 스케줄링 개론

> 소스: 이번 시간은 강의 두 건이 연속으로 진행됨. 전반부는 기존 슬라이드(`L01-linuxprocesses.pdf`) 마무리 — lightweight process(clone/do_fork), kernel thread, process 0/1, 프로세스 소멸. 후반부는 새 슬라이드(`L02-processscheduling.pdf`, 강의 직전 새로 업로드된 버전이라고 공지)로 넘어가 프로세스 스케줄링을 시작. 초반에 지난 시간(9/9)의 리스트/해시, run queue/wait queue, process switching 4대 이벤트, thread_struct/switch_to를 짧게 복습.

### 28. Lightweight Process — 스레드를 별도 자료구조 없이 구현한 이유

- **동기**: 스레드 없이 순수 멀티프로세스만으로 애플리케이션 내부 병렬성을 살리면 비효율적 — 프로세스끼리는 데이터를 공유하지 않으므로 정보를 주고받으려면 **IPC**를 써야 하는데, IPC는 다루기 어렵고 비효율적. 그래서 커널 개발자들이 스레드 기능을 도입하고 싶어 했다는 것이 리눅스에 스레드가 들어온 이유.
- **구현 방식 두 가지의 실질적 차이(재확인)**: user-level thread는 커널이 그 존재를 전혀 모름 — 하나의 while 루프 안에서 if문으로 여러 작업을 분기하는 식으로 사용자 레벨에서만 스레드처럼 보이게 만든 것. 문제는, 그중 한 유저 레벨 스레드가 I/O 때문에 블록되면 커널은 이 프로그램 전체를 "스레드 하나"로 보므로 나머지 스레드들도 CPU에서 스케줄될 수 없다. Kernel-level thread는 커널이 여러 스레드를 직접 인식·관리하므로 전부 개별적으로 스케줄 가능 — 한 스레드가 I/O로 블록돼도 나머지는 계속 돈다. 이 차이가 결정적이라서 **리눅스는 kernel-level thread를 택함**.
- **설계 결정(Linus Torvalds)**: 스레드를 위해 프로세스와 별도인 새 자료구조·클래스·함수를 새로 도입하는 방법도 있었지만, **리누스 토르발스는 커널을 스레드용으로 확장하지 않기로 결정**하고, 대신 프로세스가 자원을 공유할 수 있게 허용하는 쪽을 택했다. 즉 **프로세스와 스레드를 위한 자료구조가 따로 있는 게 아니라 단 하나의 자료구조(task_struct)로 둘 다 구현**한다. 프로세스 생성 시 부모가 자식과 어떤 자원을 공유할지 지정할 수 있고, 이 설정에 따라 자식이 "프로세스"가 될 수도 "스레드"가 될 수도 있다.
- **"lightweight process"라는 이름의 의미**: 이런 이유로 스레드는 "가벼운 프로세스(lightweight process)"라고도 부를 수 있다 — 일부 자원이 공유되어 프로세스가 다 가져야 할 전체 자원·전체 자료구조를 다 갖지 않기 때문에 "가볍다".
- **Thread group**: lightweight process들은 thread group으로 묶인다. 그룹의 첫 프로세스(리더)가 **thread group leader**이고, 그 PID가 곧 **thread group ID(TGID)**가 된다. 같은 그룹에 속한 프로세스들은 "스레드"라고 부를 수 있고, 서로 다른 TGID를 가진 프로세스들은 아무것도 공유하지 않는다. **예시(Firefox)**: 현대 웹 브라우저는 멀티프로세스+멀티스레드 구조 — Firefox를 실행하면 한 프로세스 안에 여러 스레드가 있고, 이들은 같은 TGID를 공유함으로써 lightweight process 구현 방식으로 스레드임이 드러난다.

### 29. `clone()` — wrapper 함수와 do_fork로의 연결

- `clone()`은 **C 라이브러리에 정의된 wrapper 함수**로, 새 프로세스/새 스레드 생성에 쓰인다(사실상 스레드와 프로세스는 거의 같은 방식으로 만들어짐). 파라미터: 새 프로세스가 시작될 때 실행할 함수, 그리고 상태·플래그·그 함수에 넘길 인자.
- **핵심**: `clone()` 호출 시 **커널 컨텍스트를 어느 정도까지 공유할지를 플래그로 결정**할 수 있다. 스레드끼리 공유하는 대표적인 것들 — virtual address space, 파일 시스템 정보, 열린 파일 디스크립터. 새 자식이 이것들을 부모와 공유하도록 지정하면 **스레드**가 되고, 공유하지 않도록 지정하면 **프로세스**가 된다. 즉 프로세스냐 스레드냐는 **공유의 정도**로 결정된다. (시그널 관련 정보를 지정하는 플래그도 있지만 이는 스레드가 아니라 프로세스를 위한 것.)
- **동작 흐름**: 사용자 프로그램이 `clone()`을 호출 → 내부적으로 사용자 주소 공간에서 시스템 콜 호출 → 시스템 콜이므로 모드 스위치 발생 → 커널 모드에서 `do_fork()` 계열 커널 함수를 호출.
- `CLONE_VFORK` 플래그가 명시적으로 언급됨 — `vfork()` 시스템 콜과 관련(29번 참고, 상세는 31번 do_fork 설명에서 다룸).

### 30. Less Lightweight Process — `fork()`의 Copy-on-Write와 `vfork()`

- Lightweight process(스레드) 개념과 별개로, 리눅스는 **일반 프로세스** 생성 시에도 자원을 아끼기 위한 장치를 둔다 — 이를 **less lightweight process**라고 부름. 목적은 효율성: 커널 개발자들은 프로세스나 스레드를 새로 만들 때 자원을 낭비하고 싶지 않았다.
- **`fork()`와 Copy-on-Write(COW)**: `fork()` 호출로 자식 프로세스가 생기면 자식은 자기만의 가상 주소 공간을 갖지만, 그 순간 부모의 코드·데이터·스택을 전부 물리 메모리로 복사할 필요는 없다 — 생성 시점에는 부모와 자식이 **같은 물리 페이지를 참조**해도 되기 때문. 자식이 그 데이터를 읽기만 한다면 물리 메모리에 변화가 없으므로 굳이 복사할 이유가 없다. 하지만 **부모든 자식이든 어느 한쪽이 그 영역에 쓰기를 시도하는 순간**, 커널이 그 페이지를 새로운 물리 페이지로 복사한다. 이렇게 "쓰기가 발생할 때 비로소 복사한다"는 방식이 **Copy-on-Write(COW)**다.
- **`vfork()`**: 부모가 `fork()` 직후 바로 `exec()`를 호출할 용도로 쓰는 시스템 콜. 두 경우를 구분: (1) 부모가 `fork()`만 호출 — 이땐 부모와 자식이 같은 프로그램을 실행 중이므로 데이터 구분이 의미 있음. (2) 부모가 이어서 `exec()`를 호출 — 자식이 완전히 새 프로그램을 실행하게 됨. `vfork()`는 (2)를 위한 것: `exec()`가 호출되기 전까지는 부모와 자식이 메모리 주소를 공유해도 된다 — 어차피 새 프로그램이 시작되기 전이므로 부모 관련 데이터를 복사할 이유가 없다(자식은 곧 완전히 새 프로그램이 될 것이므로 부모의 데이터는 자식에게 애초에 쓸모가 없다). `vfork()` 호출 시 **부모의 실행은 블록**된다 — 자식이 종료되거나 새 프로그램을 exec할 때까지 부모가 기다린다.
- `fork()`와 `vfork()` 둘 다 결국 **같은 커널 함수**(원래 lightweight process, 즉 스레드를 구현하기 위해 도입된 함수)로 구현된다 — 오늘날은 이 함수가 부모-자식 간 공유 정도를 설정하는 용도로 fork/vfork에도 재사용된다. 교수의 정리: **리눅스가 스레드를 우아하게(elegant) 구현했다는 게 핵심** — 스레드란 결국 커널 컨텍스트를 공유하는 프로세스일 뿐이기 때문.

### 31. `do_fork()` 함수 — 프로세스 생성의 실제 처리 단계

`do_fork()`가 실제 프로세스 생성을 처리하는 커널 함수. 단계:
1. 자식을 위한 **새 프로세스 ID(PID)** 할당.
2. **`copy_process()`** 호출 — 부모의 프로세스 디스크립터(`task_struct`)를 자식용으로 복사. 부모·자식이 일부 데이터를 공유할 수도 있기 때문에 복사가 필요. 이때 새로운 `thread_struct`뿐 아니라 **새 커널 모드 스택**도 함께 설정(각 스레드/프로세스는 자기만의 커널 모드 스택을 가져야 하므로).
3. 사용자가 허용된 프로세스 개수를 초과하는지 검사 — 시스템에 프로세스가 너무 많아지는 걸 막기 위한 한도. 실제로는 거의 발생하지 않는 드문 경우지만, 바이러스·악성코드가 프로세스를 대량 생성해 시스템을 망가뜨리려는 상황에 대비한 **안전장치**.
4. 파일 디스크립터, 페이지 테이블, 그 외 프로세스/프로그램/커널 컨텍스트 관련 항목들을 복사.
5. 자식의 상태를 **`TASK_RUNNING`**(실행 가능/ready 상태)으로 설정.
6. **부모-자식 관계 및 thread-group 관계 설정** — 각 `task_struct`는 부모·형제·자식을 가리키는 포인터를 가지므로, 이 계층 구조를 새 프로세스 생성 시점에 맞게 세팅.
7. (초기화 완료 후) 새 프로세스 디스크립터를 **run queue 중 하나에 삽입**(CPU가 여러 개이고 CPU마다 여러 run queue를 가질 수 있으므로 run queue도 여러 개 존재 가능).
8. **`CLONE_VFORK`** 플래그가 지정된 경우: `vfork()`는 자식이 곧 새 프로그램으로 교체될 것을 전제하므로, 부모를 **wait queue**에 넣어 대기시킨다. 자식이 새 프로그램을 시작한 뒤에야 부모가 다시 run queue로 돌아온다.
9. 이 단계가 끝나면 `do_fork()`는 **자식의 PID를 반환**하며 종료.

### 32. Kernel Thread — "kernel-level thread"와 헷갈리면 안 되는 개념

- 지금까지 다룬 프로세스는 전부 Firefox·PowerPoint·리더 프로그램 같은 **일반 사용자 프로세스**였다. 하지만 커널(OS) 자체도 디스크 캐시 플러시, 사용하지 않는 페이지 프레임 스와핑 아웃처럼 **백그라운드에서 시간이 걸리는 일**들을 처리해야 하고, 이를 위해 커널도 스레드가 필요하다 — 이것이 **kernel thread**.
- **용어 주의(명시적으로 강조됨)**: **kernel thread**와 **kernel-level thread**는 완전히 다른 개념이다. Kernel-level thread는 (29~32번에서 다룬) 스레드 구현 방식 중 하나로, **일반 사용자 프로그램의 스레드**와 관련된 개념. 반면 지금 말하는 kernel thread는 사용자 프로그램과는 전혀 무관하며, **실질적인 커널 함수 자체를 처리**하는 존재다.
- Kernel thread도 lightweight process로 구현된다 — 자기만의 프로세스 디스크립터를 가지며 스케줄 가능하다. 다만 **일반 유저 모드가 없다**는 점이 일반 프로세스와 다르다: kernel thread는 오직 커널 모드에서만 돌고, 유저 모드 주소 공간이 없다. 다른 커널 스레드들과 커널 주소 공간, 그리고 파일시스템·파일 디스크립터 등 다른 커널 자료구조를 공유한다.
- **일반 프로세스 vs kernel thread**: 일반 프로세스는 **시스템 콜을 통해서만** 커널 함수를 실행하는 반면(부수적으로: 사용자 프로그램이 OS 도움이 필요할 때 — 시스템 콜이나 하드웨어 인터럽트 — 커널 모드로 전환하되, 시스템 콜은 그 사용자 프로그램을 "대신해서" 처리하는 것이고, 인터럽트는 특정 사용자 프로그램을 위한 게 아니라 매우 긴급해서 아주 짧게 처리되는 것이라는 차이가 있음), kernel thread는 **하나의 특정 커널 C 함수를 직접 실행**한다 — 스와핑·캐시 플러시 같은 장기 실행 커널 작업이 대표적이며 유저 프로세스와는 무관하다.

### 33. Kernel Thread vs Regular Process — `mm` 필드와 가상주소공간

- **가상주소공간 차이**: x86 기준 가상 주소 공간은 총 4GB — 유저 모드가 3GB, 커널 모드가 1GB. 일반 프로세스는 유저 모드와 커널 모드를 오가므로 4GB 전체를 사용하지만, kernel thread는 유저 모드가 없으므로 **`PAGE_OFFSET`보다 큰 선형 주소(즉 커널 모드용 1GB 영역)만 사용**한다.
- (혼동 주의) OS 코드·자료구조가 프로세스마다 복사되는 게 아니다 — **모든 프로세스가 OS의 자료구조와 코드를 공유**한다. 즉 각 프로세스의 "커널 모드 영역"은 실제 OS로 가상으로 연결되어 있는 것이지, 프로세스마다 OS를 따로 갖는 게 아니다.
- **구별 방법(`task_struct`의 필드로 판별)**: `task_struct`에는 `mm`, `thread` 같은 필드가 있다. `thread` 필드(타입 `thread_struct`)는 컨텍스트 스위칭용으로, 스택 포인터 등 하드웨어 종속 정보를 담는다. `mm` 필드는 코드·데이터·스택 같은 **가상 주소 공간(유저 모드 영역)**을 기술한다. **어떤 task의 `mm`이 NULL 포인터라면 그 task는 kernel thread다** — kernel thread는 유저 모드 영역이 없기 때문. 이것이 일반 프로세스와 kernel thread를 구분하는 실질적인 방법.

### 34. Process 0(swapper)와 Process 1(init) — 특별한 두 커널 스레드

- 전형적인 시스템에는 kernel thread가 다수 존재하는데, 그중 대표적인 두 개가 **Process 0**과 **Process 1**.
- **Process 0 = "swapper"**: **모든 프로세스의 조상**. 리눅스 부팅 초기 단계에 `start_kernel()` 함수에 의해 생성됨. 하는 일: 커널에 필요한 일부 데이터 구조를 초기화하고, 인터럽트 같은 커널 기능을 활성화하며, 또 다른 kernel thread인 **init 프로세스를 생성**한다. init을 생성한 뒤 swapper는 `cpu_idle()` 함수를 실행해 **HLT 명령어를 반복 실행** — 이는 이 프로세스가 CPU에서 돌고 있다는 것이 곧 "CPU가 idle 상태"라는 뜻. **`TASK_RUNNING` 상태의 다른 프로세스가 하나도 없을 때만** 스케줄러가 process 0(swapper)을 선택한다(즉 진짜로 idle할 때만). 멀티 CPU 시스템에서는 **CPU마다 자기만의 swapper 프로세스**가 있다 — 각 CPU가 개별적으로 idle 상태에 들어갈 수 있기 때문.
- **Process 1 = "init"**: swapper가 `init` 함수를 실행해 생성. 커널 초기화의 나머지를 완료하는 역할 — 예컨대 메모리 캐시 스와핑 등을 처리하는 다른 kernel thread들(예: `bdflush` 등)을 만들어낸다. **init은 절대 종료되지 않는다** — 모든 프로세스를 생성·감시(monitor)하는 역할을 계속 수행하기 때문에 항상 실행 중이며, 리눅스 커널에서 매우 중요한 kernel thread다.

### 35. 프로세스 소멸(Destruction) — Termination과 Removal, 두 단계

- 프로세스 종료를 **유발**하는 방법은 두 가지: 프로세스 자신이 **`exit()`**를 호출하거나, 터미널에서 kill 같은 조작으로 **시그널**을 보내 커널이 그 시그널을 받고 해당 프로세스를 종료시키는 경우. 다만 종료를 어떻게 유발했든, **소멸 자체는 항상 두 단계**로 이뤄진다: **(1) process termination**과 **(2) process removal** — 이 둘은 서로 다른 단계다.
- **1단계 — `exit()`에 의한 termination**: 커널이 종료 중인 프로세스에 대한 참조를 커널 자료구조에서 **대부분(전부는 아님) 제거**한다 — 가상주소공간(`mm`) 관련, 열린 파일, 파일시스템, 시그널 처리 관련 참조 등. 그리고 이 프로세스가 사라지므로 부모·자식·형제 관계도 갱신한다. 이 시점에서 프로세스는 **좀비(zombie)**가 된다.
- **Orphan process(부수적으로 언급)**: 종료 중인 프로세스에게 자식이 남아 있다면(정상적으로는 잘 일어나지 않음 — 부모는 보통 자식들이 끝날 때까지 기다린 뒤에야 자신도 종료하므로 매우 드문 경우) 그 자식들은 부모를 잃게 되고, **init 프로세스의 자식으로 재편입**된다 — 이를 **orphan process**라 부른다. (좀비와 헷갈리면 안 됨: 좀비는 "종료 중인 프로세스 자기 자신"이고, orphan은 "그 프로세스의 자식들"이다.)
- **좀비(zombie)란**: 이 단계에서 데이터 일부가 이미 제거되어 사실상 "종료된" 것이나 다름없지만, 완전히 사라진 건 아니라서 "죽었지만 아직 살아있다"는 의미로 zombie라 부른다. 보통 이 zombie 상태는 매우 짧고, 금방 제거된다.
- 이 단계 이후 커널은 **`schedule()`**을 호출해 다음에 돌릴 프로세스를 고른다(방금 프로세스 하나가 끝났으니 CPU에 새 프로세스가 필요하므로). 하지만 좀비는 여전히 "살아있는" 상태이므로 결국 제거해야 함 — 문제를 일으킬 수 있기 때문. 이것이 2단계의 역할.
- **핵심 규칙**: **Unix 커널은 프로세스가 종료되자마자 그 데이터를 바로 버릴 수 없다** — 부모 프로세스가 종료된 자식에 대한 정보(예: 종료 상태)를 필요로 할 수 있기 때문. 그래서 부모가 이 좀비를 가리키는 **`wait()`류 시스템 콜을 호출한 뒤에야** 비로소 2단계가 진행된다.
- **2단계 — Removal (`release_task()`)**: 좀비의 프로세스 디스크립터(`task_struct`)를 OS가 해제(release)한다 — 이제 이 데이터가 더 이상 필요 없다고 간주되기 때문. 또한 프로세스당 할당됐던 **8KB 커널 모드 메모리 영역**(커널 스택을 담고 있던 그 영역 — 20번 항목에서 다룸)도 함께 해제한다. 이 두 가지(`task_struct`, 8KB 커널 메모리 영역)가 모두 제거되면 프로세스는 비로소 **완전히 소멸**한다.

### 36. Process Scheduling 개론 — 목표와 원칙

- **동기**: 시스템에는 여러 프로세스가 있고 모두 CPU를 원한다. 하지만 CPU 시간을 한 프로세스에만 몰아줄 수는 없다 — 그러면 나머지 프로세스들이 실행 기회를 전혀 못 얻는다. 그래서 여러 프로세스 사이에 CPU 시간을 효율적으로 배분하는 것이 매우 중요한 OS 주제.
- **스케줄링의 원리**: 여러 프로세스를 하나에서 다른 하나로 전환해가며 "동시에" 실행하는 것처럼 보이게 만드는 것. 스케줄러가 결정해야 할 두 가지는 **언제 전환할지**와 **어느 프로세스를 선택할지**. 가능한 알고리즘·방법은 많고, 커널 개발자는 그중 (하나 혹은 몇 가지) 스케줄링 알고리즘을 골라야 한다.
- **스케줄링의 목표(서로 충돌하는 목표들의 절충)**: 빠른 프로세스 응답 시간, 백그라운드 작업의 처리량(throughput), 프로세스 기아(starvation) 방지, 낮은 우선순위와 높은 우선순위 프로세스 요구사항의 조화. 결국 목표는 **여러 프로세스에게 공정한 기회를 주는 것**.

### 37. 프로세스 분류 — I/O-bound/CPU-bound, 그리고 리눅스식 분류(Interactive/Batch/Real-time)

- **전통적 분류**: **I/O-bound**(I/O 장치를 많이 쓰는 프로세스 — 예: DB 애플리케이션은 DB에 I/O 요청을 많이 보냄) vs **CPU-bound**(CPU 시간을 많이 요구 — 예: 프로그램·OS 컴파일). 교수 경험담: 예전에 자기 노트북에서 큰 리눅스 커널을 통째로 컴파일하면 3~4시간 걸렸다(요즘 노트북은 더 빠르니 더 짧게 걸릴 것).
- 프로세스 하나는 **CPU burst**(명령어 실행 구간)와 **I/O burst**(I/O 요청 구간)를 번갈아 가진다. CPU burst가 지배적이면 CPU-bound, I/O burst가 지배적이면 I/O-bound. (제시된 예시: 한 프로세스는 CPU burst가 길어 CPU-bound, 다른(아래쪽) 프로세스는 CPU burst가 짧아 I/O-bound.)
- **리눅스식 분류(다른 방식)**: **interactive**, **batch**, **real-time** 세 가지.
  - **Interactive**: 키 입력·마우스 조작 같은 I/O를 기다리는 데 시간을 많이 쓰고, 입력이 들어오면 **빠르게 응답**해야 함 — 목표 지연은 대략 **50~150ms**(교수 언급: 게이머라면 이 정도로도 부족하다고 느낄 수 있음).
  - **Batch**: 백그라운드에서 오래 도는 프로세스로 CPU를 많이 소모 — 컴파일러, DB 검색 엔진, 과학 계산, (그리고 언급대로) AI 관련 작업 등. 스케줄러는 이런 프로세스가 CPU를 너무 오래 쥐고 있다고 보고 **불이익(penalize)**을 준다.
  - **Real-time**: 정확한 데드라인이 있는 프로세스 — 비디오·오디오 애플리케이션, 드론 같은 로봇 컨트롤러. 데드라인을 반드시 맞춰야 하고, 낮은 우선순위 프로세스에 의해 절대 블록되면 안 되며, 매우 높은 응답성이 요구됨.
  - 스케줄러는 배치 프로세스를 분석해 불이익을 주는 대신 **interactive 프로세스를 우대**해 사용자에게 반응성 좋은 경험을 준다.

### 38. 리눅스 스케줄링 원칙 — Preemptive Scheduling과 Time Quantum

- 스케줄러는 **언제, 어떻게** 다음에 실행할 프로세스를 고를지 결정해야 한다.
- **Preemptive scheduling**: 리눅스는 선점형 스케줄링을 쓴다 — 현재 실행 중인 task가 아직 남은 time slice가 있어도, 스케줄러가 판단하기에 더 우선순위 높은(또는 더 적합한) 다른 task로 교체해야 한다면 현재 task는 **선점(preempt)**당한다.
- **다중 스케줄링 정책 지원**: 리눅스는 여러 스케줄링 정책을 지원하며, 이는 **스케줄링 클래스**로 구현된다 — 실제 스케줄링 방식은 해당 task가 속한 스케줄링 클래스에 따라 달라짐.
- 일반(normal) task — interactive와 batch — 는 **task weight**에 따라 CPU 시간이 분배되고, **nice 값**이 이 weight를 결정한다(자세한 내용은 추후 강의에서 다룰 예정이라고 예고). 커널은 CPU 시간을 **time quantum(=time slice)** 단위로 각 프로세스/task에 분배하며, 한 프로세스가 자기 time slice를 다 쓰면 **나머지 모든 프로세스도 자기 time slice를 다 쓸 때까지** 그 프로세스는 다시 실행될 수 없다.
- **선점이 일어나는 두 경우(상태 다이어그램 기준)**: `TASK_RUNNING`은 ready와 running을 모두 포함하고, `TASK_INTERRUPTIBLE`/`TASK_UNINTERRUPTIBLE`은 blocked 상태를 나타낸다.
  1. 실행 중인 프로세스의 **time quantum이 만료**되면 그 프로세스는 CPU를 내놓고 ready 상태로 돌아간다.
  2. wait queue에서 대기 중이던(blocked) 프로세스가 **runnable(ready)로 바뀌었을 때**, 그 프로세스의 우선순위가 현재 실행 중인 프로세스보다 높다면 커널은 현재 프로세스를 **선점**하고 스케줄러가 다음에 돌릴 프로세스를 새로 고른다.

### 39. 스케줄러의 진화 — O(1) → CFS → EEVDF

- 리눅스 스케줄러는 많이 진화해왔다. 2001년경(교수 표현: "한국 월드컵 직후") 도입된 것이 **O(1) 스케줄러**로, 지금 기준으로는 매우 오래된 방식. 이번 강의에서 O(1) 스케줄러를 공부하는 이유는 이후 **CFS(Completely Fair Scheduler)**가 왜 채택됐는지를 이해하기 위함.
- 진화 순서: **O(1) 스케줄러 → CFS → EEVDF**(가장 최신). EEVDF는 **CFS의 확장(extension)**이며, 교수는 CFS를 "리눅스 스케줄링에 대한 거의 최선의 답"이라고 평가.
- O(1) 스케줄러는 (Red Hat 소속으로 추정되는 인물이) 이전 스케줄러의 성능 문제를 해결하기 위해 도입한 것 — "새 스케줄러는 항상 이전 스케줄러의 문제를 풀기 위해 등장한다"는 일반적 패턴. O(1) 스케줄러는 **priority-based time-sharing** 방식이지만 한계가 있어 이후 CFS로 대체됨.
- 스케줄러가 계속 갱신되는 이유: 새로운 애플리케이션·컴퓨팅 장치가 계속 등장하기 때문 — 예컨대 오늘날은 **AI 서빙/LLM** 워크로드나 **모바일 같은 자원 제약 기기**를 다뤄야 하는 새로운 요구가 있고, 이런 요구가 EEVDF 같은 새 스케줄러 도입으로 이어진다.

### 40. O(1) 스케줄러 — 세 가지 정책과 140단계 우선순위, Runqueue 구조

- O(1) 스케줄러는 **세 가지 스케줄링 정책**을 정의: **`SCHED_FIFO`**(first-in-first-out — 일반 OS 수업의 FCFS와 같은 개념), **`SCHED_RR`**(round-robin), **`SCHED_OTHER`**(일반 프로세스용).
- `SCHED_FIFO`와 `SCHED_RR`은 **real-time 프로세스 전용**(흔치 않은 특수 목적) — **supervisor mode에서만 사용 가능**, 즉 일반 사용자 프로세스는 직접 쓸 수 없다. 둘의 차이는 **time quantum 유무**: `SCHED_FIFO`는 time quantum을 지정하지 않아 프로세스가 스스로 끝낼 때까지(원한다면 영원히) CPU를 쥘 수 있고, `SCHED_RR`은 time quantum이 있어서 그것이 만료되면 CPU를 내놔야 한다.
- 나머지 일반 프로세스들은 `SCHED_OTHER`류 O(1) 정책 아래에서 동작 — 이때 커널은 **interactive 프로세스에게 좋은 응답 시간을 주기 위해 암묵적으로 I/O-bound 프로세스를 CPU-bound 프로세스보다 우대**한다(게임·타이핑처럼 지연을 줄여야 하는 경우를 배려하고, 컴파일러 같은 건 상대적으로 덜 신경 씀).
- **우선순위 체계**: 총 **140단계**의 우선순위가 있고, **숫자가 작을수록 우선순위가 높다**(교수가 직접 헷갈린다고 언급할 만큼 반직관적인 규칙이니 주의). **0~99**는 real-time task 전용(supervisor mode만 사용, `SCHED_FIFO`/`SCHED_RR`로 스케줄됨). **100~139**(40단계)는 일반(normal) task용 — 이 범위 안에서도 마찬가지로 숫자가 작을수록 우선순위가 높다.
- **Runqueue 구조**: 각 CPU는 자기만의 run queue를 가지며, O(1) 스케줄러에서 각 CPU의 run queue는 **우선순위 하나당 리스트 하나씩, 총 140개의 리스트**로 구성된다. "O(1)"이라는 이름의 이유: 스케줄러가 다음 task를 고를 때 **가장 높은 우선순위의 (비어있지 않은) 리스트 하나만 보면 되기 때문**에 항상 상수 시간이 걸린다 — 이 방식 이전에는 스케줄러가 가장 높은 우선순위 task를 찾기 위해 모든 리스트/태스크를 훑어야 했다. Runqueue 자료구조는 배열 2개와, **`active`/`expired`**라는 포인터 2개를 갖는다(자세한 동작은 42번 참고).

### 41. `SCHED_FIFO`/`SCHED_RR` 동작 방식 — 예제로 확인

- **`SCHED_FIFO`**: 실행 가능한 task 중 **가장 높은 우선순위**를 가진 것이 CPU를 잡는다. 이 task는 다음 세 가지 중 하나가 일어날 때까지 CPU를 계속 쥔다:
  1. **더 높은 우선순위의 task가 runnable이 될 때** — 현재 task는 선점되지만, 이때 선점된 task는 큐의 **맨 뒤가 아니라 맨 앞**으로 간다(맨 뒤로 보내면 같은 우선순위 그룹 안에서 불공정하기 때문 — FIFO 의미상 같은 우선순위 그룹 내에서는 여전히 순번이 지켜져야 함). 나중에 그 상위 우선순위 task가 CPU를 내놓으면, 선점됐던 task가 다시 CPU를 잡아 원하는 만큼 계속 실행할 수 있다.
  2. **현재 task가 스스로 blocked 상태가 될 때**(예: I/O 대기) — 자발적으로 큐를 떠나고, 나중에 다시 runnable이 되면 큐의 **맨 뒤**로 들어간다.
  3. **현재 task가 스스로 CPU를 양보(yield)하는 함수를 호출할 때** — `SCHED_FIFO`는 그냥 두면 영원히 돌 수 있으므로, task가 명시적으로 CPU를 내놓을 수 있는 수단이 필요하며, 이 경우에도 큐의 **맨 뒤**로 들어간다.
  - 이 세 가지가 일어나지 않는 한 실행 중인 `SCHED_FIFO` task는 CPU를 영원히 쥔다. 새로 생긴 runnable task는 항상 큐의 맨 뒤에 추가되며, 같은 우선순위를 가진 task들 사이에서는 큐의 맨 앞에 있는 task가 CPU를 얻는다.
- **예제**: real-time 프로세스 4개 — D(가장 높은 우선순위), B·C(D보다 낮은, 서로 같은 우선순위), A(이 중 가장 낮은 우선순위). `SCHED_FIFO`라면 스케줄링 순서는 단순히 **D → B → C → A**(각자 끝날 때까지 돌고 다음 우선순위 큐의 맨 앞으로 넘어감).
- **`SCHED_RR`**: `SCHED_FIFO`와 동일하되 **각 프로세스에 time slice가 주어진다는 점만 다름**. time slice가 만료되면 CPU에서 내려가 자기 우선순위 큐의 **맨 뒤**로 들어간다. 같은 예제에서: B가 돌다가 time slice가 만료되면 선점되어 큐 뒤로 가고 C가 실행되며, B·C가 한 바퀴씩 돌고 나면 다시 B, C 순으로 반복된다 — 결과적으로 순서는 **D → B → C → B → C → A**가 된다. (**time-sharing은 같은 우선순위를 가진 프로세스들 사이에서만 일어난다**는 점이 핵심 — 서로 다른 우선순위 사이에서는 여전히 상위 우선순위가 항상 이긴다.)
- 이 두 정책은 모두 supervisor mode에서 도는 real-time 프로세스용이며, **실행 가능한 real-time 프로세스가 하나도 없을 때만** 일반(normal) 클래스 프로세스가 O(1) 정책으로 스케줄된다.

### 42. Active/Expired Queue — O(1) 복잡도를 만드는 트릭

- 커널은 우선순위별로 **`active`**와 **`expired`** 두 종류의 큐를 유지한다. 아직 time slice를 다 쓰지 않은 프로세스는 **active 큐**에 있고, time slice를 전부 소진한 프로세스는 **expired 큐**로 옮겨진다.
- 스케줄링은 항상 **active 큐에서만** 이뤄진다(expired 큐의 task들은 남은 time slice가 없으므로 애초에 고를 이유가 없음). Active 큐의 프로세스들이 하나씩 time slice를 소진하며 expired 큐로 옮겨가다가, 결국 어느 시점엔 active 큐가 완전히 비고 모든 task가 expired 큐에 있게 된다.
- **이때 커널이 하는 일은 단지 포인터를 교환하는 것**: 실제로는 배열이 2개 있고, `active`/`expired`는 그 배열들을 가리키는 포인터일 뿐이다. 그래서 모든 task의 time slice가 소진되는 시점에 커널은 어느 배열이 active를, 어느 배열이 expired를 가리킬지 **포인터만 맞바꾸면** 모든 task에게 새 time slice가 부여된 것과 같은 효과를 낸다(개별 task를 일일이 새 큐로 옮길 필요가 없는 매우 우아한(elegant) 구현). 
- 강의는 여기서 시간 관계상 마무리되었고, 프로세스 스케줄링(CFS 등 이후 내용)은 **다음 강의에서 이어가겠다고 예고**됨 — 이 노트는 다음 강의 내용을 추측해서 채우지 않는다.
