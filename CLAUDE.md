# coursebook

수강 과목마다 전용 교과서를 만드는 레포. 방학에는 강의계획서만으로 전 주차를 한 바퀴 돌 수 있는 교재를 생성하고(예습), 학기 중에는 강의 녹취·강의안을 받아 주차별 복습 자료로 재정리한다. 모든 학기·과목이 이 레포 하나에 쌓인다.

## 디렉토리 규칙

```
<학기>/<course-slug>/
├── syllabus.md            # 계획서 요약: 주차표, 평가 비중, 선수과목, 교수·교재
├── 00-roadmap.md          # 코스맵: 주차→토픽→정본 소스 매핑, 깊이 벤치마크. 모든 챕터 생성의 앵커
├── weeks/NN-topic-slug/   # 주차별 교과서 (아래 챕터 템플릿)
└── semester/week-NN/      # 학기 중 생성물: notes.md, delta.md
```

- 과목 슬러그·주차 슬러그는 영어 kebab-case. 주차 번호는 2자리 (`03-data-parallelism`).
- `_private/` 는 gitignore 대상. 강의 녹음, 녹취록 원본, 교수 강의안, 계획서 PDF 원본은 반드시 여기에만 둔다. **절대 커밋하지 않는다.**
- 시험 주차(중간·기말·자율학습)는 `weeks/` 를 만들지 않는다.

## 언어 규칙

- 본문 설명은 **한국어**. 빠르게 읽는 것이 목적.
- 기술 용어·헤딩·다이어그램 라벨·코드·주석은 **영어 원어 유지**. 용어를 한국어로 번역하지 않는다 (예: "gradient bucketing을 통해", ✗ "기울기 양동이화").
- `exercises.md` 의 문제·모범답안은 **영어** — 실제 시험이 영어이므로 시험 표현에 그대로 노출되는 것이 목적.
- 챕터 말미에 영어 용어 정리(Glossary) 포함: 시험 답안에 쓸 수 있는 한 줄 영어 정의.

## 깊이 기준

개괄 금지. 탑티어 CS 학부 상급/대학원 입문 수준을 기준으로 한다. 과목별 벤치마크 강의·정본 소스는 각 과목 `00-roadmap.md` 에 고정되어 있고, 챕터는 그 소스에 근거해서 쓴다.

- 모든 주장·수치·알고리즘은 정본 소스(지정 교재, 원 논문, 공식 문서, 실제 소스 코드)에 근거를 둔다. 출처를 챕터 말미 References 에 명시한다. 확신 없는 내용은 쓰지 않는다.
- 수식이 자연스러운 주제는 수식으로 쓴다 (LaTeX `$...$`). 그림이 필요한 곳은 mermaid.
- "X는 Y다" 수준의 나열이 아니라, 왜 그렇게 설계됐는지·트레이드오프·실패 모드까지 다룬다.
- 실제 시스템/코드 기준으로 검증 가능한 서술을 한다 (예: 커널이면 실제 커널 소스 경로, ML이면 실제 PyTorch API).

## 챕터 템플릿 (`weeks/NN-slug/`)

- `README.md` — 교과서 본문
  1. **Learning goals** — 시험 가능한 서술로 ("~를 유도할 수 있다", "~의 트레이드오프를 비교할 수 있다")
  2. **Why this matters** — 이 주제가 존재하는 이유, 앞뒤 주차와의 연결
  3. 본문 섹션들 — 개념 → 메커니즘 → 트레이드오프/실패 모드. worked example 포함
  4. **Common misconceptions** — 흔한 오개념·시험 함정
  5. **Glossary** — 영어 용어 한 줄 정의
  6. **References** — 논문/교재 챕터/코드 경로, 링크
- `exercises.md` — 시험 스타일 연습문제 (영어). 단답·서술·계산·설계 문제 섞어서 8~12문항, 배점 표기, 접기(`<details>`)로 모범답안.
- `quiz.md` — active recall Q&A 은행 15~25문항. 마지막 섹션에 Anki 임포트용 TSV 코드블록 (`question<TAB>answer`).
- `lab/` — 실행 가능한 실습. `README.md`(목표·실행법·예상 출력) + 코드. 의존성은 lab 안에서 해결 (`requirements.txt` 등). 실행 환경이 특수하면(VM 등) 셋업 절차를 문서화.

## 학기 중 워크플로

1. 강의 녹음·강의안을 `_private/<학기>/<course-slug>/week-NN/` 에 넣는다.
2. `/ingest-lecture` → 전사 → `semester/week-NN/notes.md`(구조화 강의 노트) + `delta.md`(예습 교재와의 차이: 교수가 강조한 것, 교재에 없던 것, 정정된 것) 생성.
3. delta 는 시험 신호다. `/exam-prep` 이 delta 를 가중해서 시험 대비 자료를 만든다.

## 작업 규칙

- 챕터를 새로 만들거나 고칠 때는 반드시 해당 과목 `00-roadmap.md` 와 이 파일을 먼저 읽는다.
- 챕터 생성 후 lab 코드는 실제로 실행해서 검증한다 (VM 필요 등 불가능하면 빌드 절차 문서화까지).
- 커밋 메시지는 `<course-slug>: <what>` 형식 (예: `system-programming: add week 02 chapter`).
