# Toy application source. The experiment edits this file to show
# which Dockerfile ordering survives a source-only change with its
# dependency-install layer cache intact.
print("hello from main.py")
